{
  "name": "mqtt_server",
  "description": "Receives mqtt messages and forwards them to the specified service.",
  "version": "0.0.1",
  "dependencies": {
    "distribution": "1.0.0",
    "mqtt": "~0.3.12",
    "underscore": "~1.7.0",
    "winston": "^0.8.1"
  },
  "devDependencies": {
    "mocha": "~2.0.1",
    "jshint": "~2.5.6",
    "should": "~4.1.0"
  },
  "scripts": {
    "start": "node server.js",
    "debug": "node-debug server.js",
    "lint": "./node_modules/.bin/jshint . --reporter=./node_modules/jshint-full-path/index.js",
    "integration-test": "mocha --reporter spec -r should"
  }
}
